const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const hom = await prisma.homologacion.findMany({ where: { estabBase: { contains: 'CASA CENTRAL' } } }); console.log('Hom:', hom); const sin = await prisma.siniestro.findMany({ where: { estabBase: { contains: 'CASA CENTRAL' } }, select: { id: true, estabBase: true } }); console.log('Sin:', sin.length); } main().finally(() => prisma.$disconnect());
